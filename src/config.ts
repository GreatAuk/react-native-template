const config = {
  server: {
    API_DOMAIN: 'service-url', // API 域名
    SESSION_TIMEOUT: 10000, // API 超时
  },
  app: {
    AMAP_IOS_KEY: '----',
    AMAP_ANDROID_KEY: '---',
  },
};

export default config;
